# B2B 区域化销售与线索运营验证记录

## 2026-08-24

英文 `/en/contact/` 页面已呈现采购资料与销售跟进两个字段组。受控客户端核验确认：国际市场偏好为 `international`、默认跟进渠道为 `email`、默认采购意图为 `quote`，全站渠道归因函数 `window.__trackSalesChannel` 已加载。本轮未提交真实询盘，避免向现有销售自动化与通知渠道写入测试线索。

中文 `/cn/contact/` 路由已呈现中文销售跟进字段，默认沟通方式为 `wechat`、采购意图为 `quote`。核验发现浏览器语言与页面语言可能不同，因此市场偏好逻辑已调整为优先使用当前站点语言，再以浏览器语言作为后备判断。

最终受控核验：中文页面的语言代码为 `zh-CN`，现已被正确识别为 `china` 市场偏好与 `CN` 区域，首选跟进方式保持为 `wechat`。
