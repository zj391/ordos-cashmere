# 产品详情页精致化任务

- [x] 审阅详情页布局、数据字段、客户端脚本与结构化数据。
- [x] 确定高端欧美 B2B 的信息层级、视觉细节和询盘动线。
- [x] 实施产品详情页的视觉与交互优化，保留全部内容、路由和多语言能力。
- [x] 验证多语言产品详情页、结构化数据、可访问性与 Astro 构建。
- [x] 提交并同步经过验证的优化版本至 `master`。

# B2B 采购询盘闭环（第一阶段）

- [x] 审计现有联系页、快速询盘、询盘清单与服务端处理链路。
- [x] 定义采购方资格字段、产品上下文和销售跟进信息。
- [x] 实施低摩擦的询盘表单、提交状态与销售上下文传递。
- [x] 验证多语言入口、产品预填、表单提交与 Astro 构建。
- [ ] 提交并同步经过验证的首阶段版本至 `master`。

# B2B 采购资料与信任证明中心（第二阶段）

- [x] 审计下载页、认证、质检、样品、物流与付款说明的现有内容和链接。
- [x] 定义采购资料包、信任证明和下一步询盘的内容层级。
- [x] 实施资料下载中心和关键采购说明的跨页面入口优化。
- [x] 验证资料链接、多语言页面与 Astro 构建。
- [ ] 提交并同步经过验证的第二阶段版本至 `master`。

> 界面核验（2026-08-24）：英文资料中心已呈现四段式采购核验路径、资料包申请入口和六类资料请求卡；卡片文案明确为人工审核后的邮件资料交付，不再暗示不存在的直接文件下载。

# B2B 采购选品效率（第三阶段）

- [x] 审计目录筛选、代表款数据、产品标签与询盘清单能力。
- [x] 定义材质、细度、针型、MOQ、交期与贴牌能力的采购筛选维度。
- [x] 实施采购筛选增强、产品比较视图与询盘清单衔接。
- [x] 验证筛选结果、比较操作、多语言页面和 Astro 构建。
- [ ] 提交并同步经过验证的第三阶段版本至 `master`。

# B2B 区域化销售与线索运营（第四阶段）

- [x] 审计区域识别、来源参数、线索评分与现有联系入口。
- [x] 定义区域化行动路径、采购意图分类与销售跟进字段。
- [x] 实施区域化入口、来源归因和销售上下文增强。
- [x] 验证区域入口、来源信息、多语言路径与 Astro 构建。
- [ ] 提交并同步经过验证的第四阶段版本至 `master`。

# B2B 销售运营看板与响应 SLA（第五阶段）

- [x] 审计现有后台路由、线索数据、权限控制和状态字段。
- [x] 定义销售运营指标、筛选维度、跟进状态与响应 SLA。
- [x] 实施受保护的销售看板、线索筛选与状态管理增强。
- [x] 验证后台访问控制、数据展示、状态更新与 Astro 构建。
- [ ] 提交并同步经过验证的第五阶段版本至 `master`。

# B2B 报价与样品协同工作流（第六阶段）

- [x] 审计询盘详情、管理接口、订单与样品相关数据能力。
- [x] 定义报价草稿、样品进度和转化阶段的安全状态模型。
- [x] 实施受保护的报价与样品协同界面及状态保存。
- [x] 验证后台访问、状态更新、历史记录与 Astro 构建。
- [ ] 提交并同步经过验证的第六阶段版本至 `master`。

# B2B 客户协同与复购运营（第七阶段）

- [x] 审计线索、询盘、客户记录与后台关联能力。
- [x] 定义客户协同视图、重点客户节奏与资料版本状态模型。
- [x] 实施受保护的客户协同页面与客户服务记录保存。
- [x] 验证后台访问、客户记录、服务节奏和 Astro 构建。
- [ ] 提交并同步经过验证的第七阶段版本至 `master`。

# Vercel Hobby Serverless Functions 限制修复

- [x] 审计根级函数入口、Vercel 配置和当前构建产物数量。
- [x] 设计兼容现有 API 路径的函数聚合与路由重写方案。
- [x] 实施函数入口合并并保留现有前端和后台调用路径。
- [x] 验证函数数量、关键接口响应和 Vercel 构建输出。
- [x] 提交修复并说明 Vercel 重新部署步骤。

# 可抓取羊绒科普博客内容体系

- [x] 审计现有博客、文章集合、站点地图和 Article 结构化数据。
- [x] 研究可靠的羊绒科普主题并确定首批内容集群与检索意图。
- [x] 撰写首批可抓取英文科普文章，补充内部链接、作者与 Article 元数据。
- [x] 验证博客静态输出、文章元数据、sitemap 与 robots 抓取路径。
- [x] 提交并同步经过验证的科普内容版本至 `master`。

# 技术 SEO 与国际化抓取优化

- [x] 审计静态构建产物中的 canonical、robots、hreflang、JSON-LD、sitemap 和内部链接信号。
- [x] 确定不会改变既有页面、路由和多语言内容的高优先级修复方案。
- [x] 实施技术 SEO、国际化规范链接、结构化数据或内容内链优化。
- [x] 验证改动后的 HTML 抓取信号、站点地图、Astro/Vercel 构建与函数数量。
- [x] 提交并同步经过验证的 SEO 优化版本至 `master`。

# 产品与分类页 SEO 优化

- [x] 审计产品详情、分类目录、筛选状态、canonical、Product JSON-LD 和 sitemap 输出。
- [x] 确定产品重复风险、富结果字段和站内链接的高优先级兼容性修复方案。
- [x] 实施不改变产品数据、真实详情 URL 或现有多语言结构的 SEO 优化。
- [x] 验证产品页面 HTML、结构化数据、sitemap、Astro/Vercel 构建与函数数量。
- [x] 提交并同步经过验证的产品 SEO 优化版本至 `master`。

# 产品分类枢纽 SEO 与采购内容优化

- [x] 审计五个产品分类页的主题内容、可抓取内链、FAQ 与采购意图覆盖。
- [x] 清理未经独立验证的买家评价、评分或客户背书，并用可核验的采购信息替代。
- [x] 设计分类页的采购决策内容、支持文章和锚文本体系。
- [x] 实施不改变既有分类与产品详情 URL 的内容、内链和结构化数据优化。
- [x] 验证分类页 HTML、内部链接、JSON-LD、sitemap、Astro/Vercel 构建与函数数量。
- [x] 提交并同步经过验证的分类枢纽 SEO 版本至 `master`。

# 采购信任页 SEO 与核验链路优化

- [x] 审计工厂、质量控制、物流与付款页面的内容覆盖、可信度声明、FAQ 与内部链接。
- [x] 修复质量控制、物流与付款页的跨语言主题错配，并移除不可核验的运营规模、认证、平台或客户背书声明。
- [x] 设计规格核验、交付条款和询盘前准备的主题链接与锚文本体系。
- [x] 实施不改变既有信任页 URL、产品数据或多语言路由的内容与内链优化。
- [x] 验证信任页 HTML、内部链接、结构化数据、sitemap、Astro/Vercel 构建与函数数量。
- [x] 提交并同步经过验证的采购信任页 SEO 版本至 `master`。

# 品牌实体、案例与询盘入口 SEO 优化

- [x] 审计案例、关于我们和联系页的实体信息、作者或来源说明、canonical、JSON-LD 与可抓取内链。
- [x] 识别并移除不可核验的客户、项目、业绩、团队或联系方式声明，修复跨语言内容错配。
- [x] 修复全站 Organization 与 ContactPage Schema 中不可核验的规模、社交、服务范围、分类代码或响应时效声明。
- [x] 实施不改变既有 URL、真实产品数据或多语言路由的品牌内容、内链和询盘承接优化。
- [x] 验证品牌页 HTML、结构化数据、国际化信号、sitemap、Astro/Vercel 构建与函数数量。
- [x] 提交并同步经过验证的品牌与询盘 SEO 优化版本至 `master`。

# 服务 Schema 与私牌 OEM 页面 SEO 优化

- [x] 审计服务 Schema、私牌 OEM 页面、相关元数据与可见内容中的服务范围、MOQ、交期和能力声明。
- [x] 识别并移除不可核验或与页面主要内容不一致的服务范围、产品规格、认证、报价或时效结构化数据。
- [x] 实施不改变既有服务 URL、产品路由或多语言结构的采购信息、内链和书面报价承接优化。
- [x] 验证服务页面 HTML、JSON-LD、国际化信号、sitemap、Astro/Vercel 构建与函数数量。
- [x] 提交并同步经过验证的服务内容 SEO 优化版本至 `master`。

# 核心入口页元数据与采购承接 SEO 优化

- [x] 审计首页、原料、纱线、面料与成衣 OEM 页的标题、描述、可见商业声明、JSON-LD 和内链。
- [x] 识别并移除不可核验的认证、产能、服务地区、免费样品、MOQ、交期或价格类摘要声明。
- [x] 实施不改变既有 URL、产品数据或多语言结构的主题元数据、采购内容和内部链接优化。
- [x] 验证核心入口页 HTML、JSON-LD、国际化信号、sitemap、Astro/Vercel 构建与函数数量。
- [x] 提交并同步经过验证的核心入口页 SEO 优化版本至 `master`。

# 支持内容页与工具页 SEO 优化

- [x] 审计下载中心、FAQ、隐私页、博客汇总和产品比较页的标题、描述、可见商业声明、JSON-LD 与内链。
- [x] 识别并移除不可核验的认证、报告、资料交付、更新频率、订单条款、价格或比较功能承诺。
- [x] 实施不改变既有 URL、产品数据或多语言结构的可信摘要、采购承接和内部链接优化。
- [x] 验证支持内容页 HTML、JSON-LD、国际化信号、sitemap、Astro/Vercel 构建与函数数量。
- [x] 提交并同步经过验证的支持内容页 SEO 优化版本至 `master`。

# 产品详情页商业字段与结构化数据 SEO 优化

- [x] 审计产品详情页可见价格、MOQ、交期、样品、库存字段及 Product JSON-LD 输出。
- [x] 识别并移除不可核验或与页面主要规格不一致的 Offer、库存、价格、MOQ、交期和样品承诺。
- [x] 实施不改变真实产品详情 URL、材质规格、图片或多语言结构的项目级书面确认与内部链接优化。
- [x] 验证产品详情 HTML、Product JSON-LD、国际化信号、sitemap、Astro/Vercel 构建与函数数量。
- [x] 提交并同步经过验证的产品详情 SEO 优化版本至 `master`。

# 颜色、原料、工艺与政策专题页 SEO 优化

- [x] 审计颜色卡、分梳羊绒、纱线类型、工艺与政策类页面的标题、可见商业声明、JSON-LD 与内链。
- [x] 识别并移除不可核验的认证、纯度、测试方法、色卡年份、价格、MOQ、交期或资料下载承诺。
- [x] 实施不改变既有专题 URL、真实知识内容或多语言结构的可信摘要、项目确认和内部链接优化。
- [x] 验证专题页 HTML、JSON-LD、国际化信号、sitemap、Astro/Vercel 构建与函数数量。
- [x] 提交并同步经过验证的专题页 SEO 优化版本至 `master`。

# 可索引页面与 sitemap 一致性 SEO 审计

- [x] 盘点 sitemap 生成逻辑、当前 noindex 路由与静态页面清单，确认不希望索引的 canonical URL 不会继续进入 sitemap。
- [x] 审计仍可索引的服务、博客、分类和辅助页面中残留的报价、库存、MOQ、交期、认证、评价、服务范围与 Offer Schema 声明。
- [x] 实施最小范围的 sitemap、robots、内容与结构化数据修复，保留真实 URL、可用采购入口与多语言结构。
- [x] 验证修复后的静态 HTML、canonical、hreflang、robots、JSON-LD、sitemap、Astro/Vercel 构建与五个 API 入口。
- [x] 提交并同步经过验证的可索引页面 SEO 修复版本至 `master`。

# 产品标题扩充与搜索语义 SEO 优化

- [x] 审计产品标题、分类、材质、真实规格、六语本地化字段及详情页 `<title>`/H1 输出路径，统计重复与空缺情况。
- [x] 制定只使用现有真实产品字段的六语标题扩充规则，避免添加认证、价格、MOQ、库存、交期、产能或未证实性能主张。
- [x] 批量实施可逆的产品标题扩充，保持全部产品 ID、URL、产品数据、图片、询盘与多语言路由不变。
- [x] 验证标题长度、唯一性、六语页面输出、canonical、hreflang、Product JSON-LD、sitemap、Astro/Vercel 构建与五个 API 入口。
- [x] 提交并同步经过验证的产品标题 SEO 优化版本至 `master`。

# 产品标题长尾语义覆盖增强

- [x] 审计现有扩充标题对款式、图案、织法、用途、版型、季节、人群和尺寸等真实字段的使用率与语义缺口。
- [x] 制定属性优先级、字段清洗和六语自然表达规则，避免关键词堆砌、过长标题或未核验的商业词。
- [x] 实施产品级属性标题增强，保持全部产品 ID、URL、图片、原始名称、材质规格、询盘和多语言路由不变。
- [x] 验证扩充后的标题唯一性、属性覆盖、长度、H1、Product JSON-LD、canonical、hreflang、sitemap、Astro/Vercel 构建与五个 API 入口。
- [x] 提交并同步经过验证的长尾标题覆盖增强版本至 `master`。

# 产品分类页视觉对比与路由可用性修复

- [x] 定位分类页白底文字对比不足的样式来源，并盘点导航、分类卡片、产品列表和静态构建中失效的分类路径。
- [x] 设计保留现有分类 URL、产品数据、语言前缀、查询筛选和询盘入口的文字对比与路由兼容修复方案。
- [x] 实施分类页前景色修复、分类入口映射或兼容路由修复，不删除真实页面或分类链接。
- [x] 验证六语分类页面的可见文字对比、H1、canonical、HTTP/静态路径、sitemap、Astro/Vercel 构建与五个 API 入口。
- [x] 提交并同步经过验证的分类页可用性修复版本至 `master`。

# 头部网站标题架构参考与长尾 SEO 增强

- [x] 研究公开头部羊绒、奢侈针织和专业 B2B 网站的分类页、产品页与标题层级，记录可借鉴的信息架构而不复制文案。
- [x] 审计本站产品与分类标题的类型词、材质、款式、织法、版型、适用人群、季节、尺寸和采购意图语义缺口。
- [x] 制定仅使用现有真实字段的六语长尾标题模板、分类页标题层级和长度/去重规则，避免关键词堆砌与未核验商业词。
- [x] 实施产品及分类页面的标题与可见标题增强，保持产品 ID、URL、路由、图片、询盘、多语言和既有 canonical 信号不变。
- [x] 验证标题质量、唯一性、语言一致性、H1、JSON-LD、canonical、hreflang、sitemap、Astro/Vercel 构建与五个 API 入口。
- [x] 提交并同步经过验证的竞品参考标题优化版本至 `master`。

# 产品列表、筛选与内部链接发现路径优化

- [x] 盘点产品列表页、查询筛选参数、产品分类导航和内容模块中的可抓取入口、canonical 与现有锚文本。
- [x] 审计列表页标题、筛选语义、内部链接和分页/参数路径的真实字段覆盖、重复风险与索引边界。
- [x] 制定基于实际产品类型、款式、织法、材质、尺寸和分类的数据驱动长尾模块与锚文本规则，避免关键词堆砌和未核验商业词。
- [x] 实施列表页可见发现路径和内部链接增强，保留所有产品 ID、URL、筛选功能、图片、询盘、多语言和 canonical 信号。
- [x] 验证筛选路径、标题、H1、robots、canonical、hreflang、Schema、sitemap、Astro/Vercel 构建与五个 API 入口。
- [x] 提交并同步经过验证的产品发现路径优化版本至 `master`。

# 博客正文、Article Schema 与内容集群可信度优化

- [x] 盘点可索引博客文章、文章模板、作者/日期/Schema 与跨语言文章链接，统计内容集群和抓取入口。
- [x] 审计正文和元数据中固定价格、MOQ、交期、认证、测试、产能、地域、免费样品、时效或未经独立核验的采购断言。
- [x] 制定保留实质科普价值的内容收敛、引用、项目级确认和主题内链规则，不删除真实知识页或产品入口。
- [x] 实施博客正文、Article Schema、作者/日期说明和内容集群锚文本优化，保持文章 slug、多语言路由和 sitemap 不变。
- [x] 验证博客 HTML、H1、canonical、hreflang、Article JSON-LD、内链、robots、sitemap、Astro/Vercel 构建与五个 API 入口。
- [x] 提交并同步经过验证的博客可信度 SEO 优化版本至 `master`。

# FAQ、资料入口与采购流程辅助页面可信度优化

- [x] 盘点可索引 FAQ、下载/资料入口、比较工具、采购流程与服务说明页面的 H1、canonical、robots、Schema 和站内链接。
- [x] 审计辅助页面中固定价格、MOQ、交期、样品、认证、测试、产能、地域、物流、付款或服务范围断言及其结构化数据。
- [x] 制定保留采购实用性、改用项目级书面确认或 noindex 的最小范围修复方案，不删除真实 URL、工具、询盘和多语言能力。
- [x] 实施辅助页面正文、FAQ/Schema、内链和索引控制优化，保持现有产品数据与路由行为不变。
- [x] 验证辅助页面 HTML、H1、canonical、hreflang、robots、Schema、sitemap、Astro/Vercel 构建与五个 API 入口。
- [x] 提交并同步经过验证的辅助页面可信度 SEO 优化版本至 `master`。

# 全局导航、页脚、首页与 Schema 索引质量优化

- [x] 盘点导航、页脚、首页、面包屑、全局 Schema 与跨页面组件中指向专题、博客、工厂、认证和资料入口的链接及实体断言。
- [x] 审计 noindex 页面、历史博客和未经核验页面是否仍被作为全局强入口、结构化数据对象或泛化商业背书输出。
- [x] 设计保留用户访问与采购路径的链接收敛、锚文本和 Schema 修复方案，避免删除真实产品、多语言、询盘或后台能力。
- [x] 实施全局链接、首页内容、面包屑或 Schema 的最小范围修复，并保留稳定 canonical 路径。
- [x] 验证全局链接、H1、canonical、hreflang、robots、JSON-LD、sitemap、Astro/Vercel 构建与五个 API 入口。
- [x] 提交并同步经过验证的全局索引质量优化版本至 `master`。

# 搜索查询数据驱动的关键词扩展优化

- [x] 规范化用户提供的查询点击、展示、点击率和平均排名数据，按品牌、产品、材料/纱线、知识问题、非相关意图和多语言需求聚类。
- [x] 评估高展示低点击、排名 8–40、已有点击和强产品匹配查询，映射到现有产品、分类、品牌、FAQ 和知识页面。
- [x] 制定仅使用现有真实产品字段、可核验知识来源与稳定页面的关键词扩展、标题层级和内部链接规则。
- [x] 实施优先机会词的产品/分类/品牌/知识页面优化，不创建薄内容、参数页、虚假认证、价格、MOQ、库存或交期声明。
- [x] 验证目标查询词覆盖、标题/H1/Schema、canonical、hreflang、robots、sitemap、Astro/Vercel 构建与五个 API 入口。
- [x] 提交并同步经过验证的数据驱动关键词优化版本至 `master`。

# 产品标题信息层级扩展优化

- [x] 审计产品目录中颜色、图案、领型、袖型、闭合方式、口袋、工艺、尺寸、包装单位、用途及其他真实字段的覆盖率和质量。
- [x] 设计按帽子、围巾、针织成衣、配饰和纱线等产品类型区分的六语属性优先级、去重、自然语序和长度规则。
- [x] 实施产品详情 H1、`<title>`、Product JSON-LD 与面包屑的更高信息量标题生成，保留原始名称、ID、URL、图片、询盘和规格数据。
- [x] 验证扩展字段覆盖、标题长度、唯一性、六语输出、H1、Product Schema、canonical、hreflang、sitemap、Astro/Vercel 构建与五个 API 入口。
- [x] 提交并同步经过验证的高信息量产品标题优化版本至 `master`。

# 分类首屏文字对比修复

- [x] 定位分类首屏标题、说明、项目符号和辅助文字在浅色背景上继承白色前景的实际样式来源。
- [x] 实施分类首屏正文、列表、项目符号和辅助文字的黑色/深灰色前景修复，保持标题、结构、内容和路由不变。
- [x] 验证六语分类页文字对比、静态 HTML、H1、canonical、sitemap 和 Astro/Vercel 构建。
- [x] 提交并同步经过验证的分类首屏文字对比修复版本至 `master`。

## 分类首屏最终渲染文字覆盖复查

- [x] 排查截图对应中文分类页首屏中说明、列表和项目符号仍显示白色的最终 CSS 规则及其优先级来源。
- [x] 为分类首屏辅助文字和列表添加独立、明确的深色前景覆盖，避免全局深色主题样式重新覆盖。
- [x] 在实际开发页面与构建 HTML 中复核中文分类页的首屏颜色计算结果及六语共享输出。
- [x] 提交并同步本轮最终渲染文字覆盖修复至 `master`。

> 实页复核（2026-08-26）：`/cn/products/accessories-cat/` 的 hero 实际背景为 `rgb(247, 243, 236)`；说明与列表文字均为 `rgb(28, 24, 19)`，项目符号为可读的金棕色 `rgb(216, 183, 135)`。页面保留 1 个 H1 与 canonical。

# 分类页手机端响应式排版检查

- [x] 在手机模拟器检查中文分类页的首屏、导航、列表、产品卡片、横向溢出和底部操作区。
- [x] 如发现问题，实施不改变内容、链接、SEO 结构或桌面布局的最小响应式修复。
- [x] 在手机与桌面视口复测分类页关键内容区和交互入口。
- [x] 提交并同步本轮已验证的检查结果或修复至 `master`。

> 响应式复测（2026-08-26）：固定 390px CSS 视口中，分类页的 `scrollWidth` 与 `clientWidth` 均为 375，越界元素清单为空。首屏、两列产品卡片、单列分类/指南/采购卡片、博客卡片、FAQ 与行动区均保持可读。桌面首屏也已复核，主导航与分类 hero 保持正常。

# 可索引页面搜索意图与内链 SEO 增量优化

- [x] 盘点当前可索引页面、主题关键词覆盖、标题/描述长度、H1、canonical、Schema 与核心内部链接的可验证缺口。
- [x] 根据真实目录字段、可访问页面和已有搜索查询审计，筛选不会引入未核验商业断言的优化机会。
- [x] 实施最小范围的页面标题、可见主题内容、上下文内链或 Schema 优化，保持真实 URL、产品数据、多语言和询盘流程不变。
- [x] 验证 HTML 抓取信号、内链目标、canonical、hreflang、robots、Schema、sitemap、构建输出与五个 API 入口。
- [x] 提交并同步本轮经验证的 SEO 增量优化至 `master`。

> 审计结论（2026-08-26）：582 条目录记录均含 `colors` 字段；此前当个别颜色值不可用时，产品页会以类别默认“可定制颜色”生成可见高亮与描述。现已收敛为只输出列出的颜色值；构建后的 591 个英文产品页中不再包含 `Custom colors`，同时保留 canonical、hreflang、sitemap 和五个 API 入口。

# 产品与分类页面真实规格差异化 SEO 增量优化

- [x] 审计产品目录中实际规格字段、详情页可见高亮、分类页发现路径与跨产品重复文本的差异化空间。
- [x] 筛选仅依赖真实材质、纱支/细度、图案、织法、尺寸、重量或包装字段的安全优化机会，不生成价格、MOQ、库存、交期、认证或服务承诺。
- [x] 实施产品页或分类页可见规格语义、内部发现路径或结构化数据的最小范围优化，保留 URL、路由、多语言与询盘能力。
- [x] 验证六语 HTML、H1、canonical、hreflang、robots、Product/Collection Schema、sitemap、构建输出与五个 API 入口。
- [x] 提交并同步本轮经验证的真实规格 SEO 优化至 `master`。

> 审计结论（2026-08-26）：69 条纱线记录包含可解析的 `100g cone`/`500g hank` 包装与相应的重量字段；78 条包装字段中其余值为“Advantages”等无规格文本或运营承诺，未使用。现将仅可解析的包装和重量作为六语产品亮点，代表性纱线页在六语 HTML 中均输出包装 2 处、重量 1 处；帽子产品页不输出空包装字段。

# 分类主题与真实规格内部发现路径 SEO 增量优化

- [x] 审计五个分类枢纽的真实规格覆盖、产品发现模块、相关指南与相邻类目内链的主题缺口。
- [x] 基于实际产品类别、材料、织法、包装、重量、款式和查询意图筛选安全的内容与锚文本机会，不创建参数页或无依据商业承诺。
- [x] 实施可见分类内容、产品发现模块或上下文内链优化，保持全部既有分类/产品 URL、多语言、canonical 与询盘能力。
- [x] 验证六语分类 HTML、H1、canonical、hreflang、robots、CollectionPage Schema、内链、sitemap、构建输出与五个 API 入口。
- [x] 提交并同步本轮经验证的分类发现路径 SEO 优化至 `master`。

> 审计结论（2026-08-26）：纱线面料辅助页保留可访问，但其固定 MOQ、库存、交期、物流与服务断言缺乏本轮独立核验，因此已设为 `noindex,nofollow` 并从 sitemap 排除。分类指南、页脚、首页与唯一可索引博客的纱线入口均已改为 `/[locale]/products/yarn/` 的真实纱线产品记录枢纽；最终构建中首页、博客和分类枢纽对旧路径的链接均为 0。

# 可索引服务、原料与支持页面可信度 SEO 增量优化

- [x] 盘点可索引服务、原料、产地、质控、物流与支持页的标题、摘要、H1、Schema、内部链接和可核验内容边界。
- [x] 定位仍含固定价格、MOQ、库存、交期、认证、产能、市场覆盖、付款、样品或物流承诺的可索引内容与入口。
- [x] 实施最小范围的安全主题收敛、noindex 或真实产品/指南内链替换，保留全部可访问 URL、路由、多语言和询盘能力。
- [x] 验证六语 HTML、canonical、hreflang、robots、Schema、sitemap、构建输出与五个 API 入口。
- [x] 提交并同步本轮经验证的可索引页面 SEO 优化至 `master`。

> 审计结论（2026-08-26）：共享 `B2BServices` 组件曾在可索引原料和成衣页面输出固定免费样品、付款比例、FOB、文件、检验与时效承诺。现已改为六项项目准备内容，只要求买方说明规格、核验、包装、文件、目的地和商务需求，并明确所有适用条件按所选项目书面确认。六语原料与成衣静态页均含项目确认文本，固定服务承诺检测结果为 0。

# 产地页与联系页实体可信度 SEO 增量优化

- [x] 审计可索引产地页的可见产地、牧户、面积、认证、规模、品质比较和供应承诺，以及联系页的实体 Schema 字段。
- [x] 筛选可由当前页面与目录资料支持的内容；对无法独立核验的可见断言或地理/组织 Schema 字段实施安全收敛或 noindex。
- [x] 保持真实 URL、产品目录、语言版本、询盘入口与可访问内容不变，实施最小范围 SEO 可信度优化。
- [x] 验证六语 HTML、robots、canonical、hreflang、JSON-LD、sitemap、构建输出与五个 API 入口。
- [x] 提交并同步本轮经验证的产地与实体可信度 SEO 优化至 `master`。

> 审计结论（2026-08-26）：产地页包含未独立核验的世界级品质比较、牧户/牧场规模、草原认证、全链路溯源和认证清单，因此保留页面可访问性但将其六语版本设为 `noindex,nofollow` 并从 sitemap 排除。联系页仍保留可见联系方式和 `ContactPoint`，但自定义 JSON-LD 已移除成立年份、法律实体、完整邮政地址与坐标字段；六语构建中移除字段命中均为 0。

# 下载、帮助、质量控制与博客页面可信度 SEO 增量优化

- [x] 审计可索引下载、帮助、质量控制与博客页面的标题、H1、元描述、FAQ/Article Schema、正文断言和内部链接目标。
- [x] 定位未经核验的测试、报告、认证、样品、物流、付款或结果承诺，并区分可保留的项目级书面确认内容。
- [x] 实施最小范围的内容收敛、noindex、Schema 或内链优化，保留可访问 URL、真实资料请求能力、多语言和询盘流程。
- [x] 验证六语 HTML、canonical、hreflang、robots、JSON-LD、sitemap、构建输出与五个 API 入口。
- [x] 提交并同步本轮经验证的支持与内容页 SEO 优化至 `master`。

> 审计结论（2026-08-26）：质量控制页的正文、FAQ 与书面确认边界可保留，但其“核验链路”此前链接到已 noindex 的色卡辅助页。现已改为可索引的付款沟通页，形成产品规格、羊绒等级、商务条件与物流准备四个有效入口。六语构建中旧色卡链接均为 0，付款、物流、等级与产品入口均存在，且 canonical 保持正常。

# 分类、产品发现页与多语言元数据一致性 SEO 增量优化

- [x] 审计可索引分类、产品发现页与主导航/页脚/上下文入口的链接目标，识别仍导向 noindex 页面或重复搜索意图的路径。
- [x] 对照真实产品目录、分类页 H1/描述、canonical、hreflang、Collection/Product Schema 和现有查询机会，筛选安全的关联优化。
- [x] 实施最小范围的真实产品发现、内链或多语言元数据一致性修复，保留 URL、多语言路由、产品数据与询盘能力。
- [x] 验证六语 HTML、H1、canonical、hreflang、robots、Schema、sitemap、构建输出与五个 API 入口。
- [x] 提交并同步本轮经验证的分类与产品发现页 SEO 优化至 `master`。

> 审计结论（2026-08-26）：首页、全局导航、页脚与产品详情页仍存在指向 noindex 产地或工厂辅助页的入口。首页产地主题入口现指向可索引的原料页，产品详情页两处“工厂参观”入口改为质量核验规划页，导航和页脚移除 noindex 产地入口。六语静态 HTML 中首页产地锚点与产品工厂锚点均为 0；原料、核验与 canonical 信号均保留。

# 真实属性覆盖与高价值搜索意图 SEO 增量优化

- [x] 审计当前搜索查询机会、可索引产品/分类页面标题、可见规格、描述和 Schema，识别真实目录属性尚未承接的高价值主题。
- [x] 验证候选属性仅来自实际产品目录或可访问页面，排除认证、价格、MOQ、库存、交期、产能、市场覆盖、样品、物流和付款承诺。
- [x] 实施最小范围的标题、可见规格、描述、内部发现或结构化数据优化，避免关键词堆砌与重复页面。
- [x] 验证六语 HTML、H1、canonical、hreflang、robots、Schema、sitemap、构建输出与五个 API 入口。
- [x] 提交并同步本轮经验证的高价值搜索意图 SEO 优化至 `master`。

> 审计结论（2026-08-26）：标题构建器已使用独立 `gender` 与 `season` 字段，但 582 条目录记录中有 307 条带受限语义标签，部分记录的这两个独立字段为空。现仅将完全匹配的人群（women/men/unisex/kids/ladies/girls/boys/baby）和季节（spring/summer/autumn/fall/winter/year-round）标签作为后备来源；不使用 `Bestseller`、`OEM/ODM` 或任何商业标签。构建后六语产品标题中均存在数百个真实人群或季节语义，代表产品的 canonical 与 hreflang 保持正常。

# 产品可见规格、Product Schema 与支持页面发现路径一致性 SEO 优化

- [x] 审计产品详情页中可见规格、产品标题、Product JSON-LD、图片替代文本和相关产品发现入口的实际目录字段一致性。
- [x] 审计可索引支持页面是否仍指向 noindex 辅助页、重复主题或缺乏真实产品/核验上下文的内部目标。
- [x] 实施最小范围的真实规格或索引内链一致性修复，不新增商业条款、认证、价格、库存、交期、物流或付款承诺。
- [x] 验证六语 HTML、H1、canonical、hreflang、robots、Product/FAQ/Article Schema、sitemap、构建输出与五个 API 入口。
- [x] 提交并同步本轮经验证的产品与支持页 SEO 优化至 `master`。

> 审计结论（2026-08-26）：产品 Product JSON-LD 已保持名称、材料、目录细度与可见标题一致，但制造商对象仍绕过全站策略输出成立年份、法律实体和地址字段。现已将该对象收敛为组织名称与站点 URL；代表性产品的六语静态 JSON-LD 均保留 Product、manufacturer、material 与 Fiber micron，移除字段命中均为 0，canonical 与 hreflang 保持正常。

# 全局实体、真实规格与分类—产品发现路径 SEO 增量优化

- [x] 审计 BaseLayout 与可索引首页/分类页的 Organization、WebSite、CollectionPage、Breadcrumb 和 FAQ Schema 实体字段一致性。
- [x] 盘点真实目录规格在产品卡片、分类枢纽和产品详情页的覆盖，并审计分类到产品、产品到分类的可抓取发现路径。
- [x] 实施最小范围的安全 Schema、真实规格呈现或可索引内链优化；不新增认证、价格、MOQ、库存、交期、产能、地域、样品、物流或付款承诺。
- [x] 验证六语 HTML、H1、canonical、hreflang、robots、JSON-LD、sitemap、构建输出与五个 API 入口。
- [x] 提交并同步本轮经验证的全局实体与发现路径 SEO 优化至 `master`。

> 审计结论（2026-08-26）：全局 Organization、WebSite、CollectionPage、Breadcrumb、FAQ 与服务讨论 Schema 均符合现有书面确认边界。产品规格表仍会对某些通用颜色字段输出“Custom color”，色板建议也会在没有实际颜色时回退到默认色板；现已收敛为仅在目录列出具体颜色时显示颜色行和色板。英文构建产物中 `Shown in: Custom color` 及通用颜色承诺均为 0，保留 459 个实际列出颜色规格行和 1113 个对应色板节点；六语代表产品的 canonical 与 hreflang 保持正常。

# 首页、分类枢纽与产品详情页发现路径 SEO 增量优化

- [x] 审计首页、五个分类枢纽和产品详情页的真实规格覆盖、锚文本、产品发现模块及双向内链缺口。
- [x] 使用实际目录类别、材料、细度、尺寸、颜色、包装、重量、织法、人群与季节字段筛选安全优化机会，排除商业承诺。
- [x] 实施最小范围的真实规格呈现或分类—产品发现路径改进，保持既有 URL、语言路由、询盘能力和 canonical 规则。
- [x] 验证六语 HTML、H1、canonical、hreflang、robots、CollectionPage/Product Schema、内链、sitemap、构建输出与五个 API 入口。
- [x] 提交并同步本轮经验证的发现路径 SEO 优化至 `master`。

> 审计结论（2026-08-27）：首页已将认证徽章、固定产品价格、产区优越性、固定地理/纤维细度、牧场/设施归属、市场覆盖、固定 MOQ 与全链路能力叙述收敛为目录中已列材质、颜色和规格的发现入口，以及项目级书面确认。首页博客模块仅展示可索引文章，避免 noindex 历史内容经首页摘要重新暴露。该收敛不宣称企业认证、产能或服务能力已经独立验证；六语构建输出中的相关风险文本和固定 USD 价格均为 0，canonical、hreflang、FAQPage、产品发现链接及 sitemap 统计保持正常。

# 分类枢纽与高机会产品搜索意图 SEO 增量优化

- [x] 审计五个分类枢纽与既有高机会查询词对应产品的真实属性、标题/H1、可见描述和上下文内链缺口。
- [x] 仅以目录中实际类别、材质、细度、颜色、尺寸、织法、图案、包装、重量、人群与季节字段筛选可索引优化机会，排除认证与商业承诺。
- [x] 实施最小范围的分类或产品发现路径、锚文本和可见规格语义优化，保持 URL、多语言路由、询盘与 canonical 规则不变。
- [x] 验证代表性六语页面的 H1、canonical、hreflang、robots、CollectionPage/Product Schema、内链、sitemap、生产构建与五个 API 入口。
- [x] 提交并同步本轮经验证的搜索意图与发现路径 SEO 优化至 `master`。

> 审计结论（2026-08-27）：五个分类枢纽已通过现有代表产品选择规则，以真实款式家族承接 turtleneck、shawl/wrap、leggings、worsted/woollen、cone 与 hank 等检索意图；相关主题词来自目录产品名称和款式匹配规则。产品详情页与分类枢纽的博客卡片现仅链接博客索引白名单，不会将 noindex 历史文章重新作为可抓取发现目标。审计同时发现远端新增的 Product Offer Schema 会将目录中的非实时价格、默认货币和硬编码库存状态输出；现已移除该 Offer 输出，保留 Product、材料、目录细度、分类、品牌与制造商对象。此结论不将任何价格、库存、交付或供货能力视为已验证事实。

# 可索引页面商业与运营断言可信度 SEO 增量优化

- [x] 审计当前 sitemap 内的核心页面、共享组件和结构化数据，定位未经目录或页面证据支撑的价格、库存、MOQ、交期、样品、付款、物流、产能、认证、市场覆盖与实体断言。
- [x] 区分可由真实产品字段支持的规格信息、可保留的项目级书面确认语句，以及需收敛、替换或设为 noindex 的可索引内容。
- [x] 实施不改变现有 URL、产品数据、多语言路由或询盘能力的最小内容、内链、Schema 或索引控制修复。
- [x] 验证代表性六语静态 HTML、H1、canonical、hreflang、robots、JSON-LD、sitemap、生产构建与五个 API 入口。
- [x] 提交并同步本轮经验证的可索引内容可信度 SEO 修复至 `master`。

> 审计结论（2026-08-27）：工厂页仍保留大面积、员工、产能、设备、认证、服务模式、市场范围与预约时效等无法由当前目录独立验证的断言，因此保留 URL 和用户访问能力，但六语版本已输出 `noindex,nofollow` 并从 sitemap 排除。全部产品详情页的工厂参观入口，以及可索引博客正文的对应关联入口，已改向质量核验准备页；FAQ 索引页首屏亦已改为羊绒采购和已列产品信息的中性定位。六语静态输出中工厂页 robots 均正常，产品页和唯一可索引博客对工厂页的链接均为 0；不将工厂规模、认证或能力视为已验证事实。

# 分类枢纽与产品详情页可见结构性 UI 改版

- [x] 审计分类枢纽与产品详情页现有首屏、规格区域、产品发现模块和询盘行动区，确定可复用的视觉与交互基础。
- [x] 设计以暖灰档案感、真实规格标签和非对称采购路径为核心的分类/详情页信息层级，避免覆盖既有产品内容、图片、URL 或路由。
- [x] 实施大幅可见的分类规格浏览、产品详情规格摘要与询盘行动区改版，并确保六语内容可读、可访问。
- [x] 在桌面及手机视口验证代表性分类和产品页的版式、链接、文字对比、横向溢出、H1、canonical、hreflang、Schema、构建和 API 入口。
- [x] 提交并同步本轮经验证的可见结构性 UI 改版至 `master`。

> 改版结论（2026-08-27）：分类枢纽首屏由单列长文案重组为主叙述加“规格索引”侧栏，直接显示真实产品记录数量、现有主题词和目录锚点；产品详情首屏重组为图像档案、产品记录卡、规格账本和并列询盘动作，账本只展示产品编号、材料/细度及目录中存在的包装、重量字段。手机端改为单列账本，悬浮智能咨询、WhatsApp 和微信入口均保留，但移动端尺寸与间距已压缩以减小阅读遮挡。桌面本地预览、390px 手机截图和生产构建均通过；未替换产品图片、未删除 URL 或询盘/联络能力，也未新增任何商业承诺。

# 产品目录浏览与询盘路径体验优化

- [x] 审计产品总目录现有首屏、筛选层级、结果卡片、批量询盘动作和手机端交互，定位影响高效发现的瓶颈。
- [x] 设计保留真实目录字段的筛选总览、结果状态和询盘路径层级，避免新增价格、库存、MOQ、交期或其他未经确认承诺。
- [x] 实施大幅可见的产品目录浏览与批量询盘体验改版，不删除现有产品记录、筛选能力、URL 或多语言路由。
- [x] 在桌面和手机视口验证目录筛选、产品链接、询盘动作、文字对比、横向溢出、H1、canonical、hreflang、Schema、构建和 API 入口。
- [x] 提交并同步本轮经验证的产品目录体验优化至 `master`。

> 改版结论（2026-08-27）：产品总目录已由分散的首屏、类别按钮、搜索/排序控件和材质筛选区，重组为连续的目录浏览控制台。首屏新增分类档案导航，按真实代表产品数量连接到五个稳定分类枢纽；控制区将类别筛选、搜索、排序、结果状态与材质筛选置于统一阅读路径，仍沿用既有客户端筛选、比较、分页和询盘逻辑。首屏副文案改为目录中已列的产品类别与项目书面确认，不新增工厂、价格或可追溯性断言。桌面与 390px 手机预览显示目录索引和筛选控制区均无横向截断；六语静态目录页仍有 H1、筛选控件、产品链接、询盘入口、canonical、hreflang 和 CollectionPage Schema。

# 全量产品真实规格标题与 H1 扩展

- [x] 审计产品目录中材料、细度、颜色、尺寸、图案、织法、包装、重量、人群和季节字段的实际覆盖，以及当前 `<title>`、H1 与 Product Schema 名称的属性使用缺口。
- [x] 定义符合行业产品命名习惯的全量字段优先级和长度控制规则，仅纳入目录中真实存在且可展示的规格信息。
- [x] 实施统一标题生成器升级，并保持 `<title>`、H1、Product Schema、面包屑及现有多语言路由的一致性。
- [x] 运行字段覆盖审计，抽检六语产品 HTML 的标题、H1、canonical、hreflang、Product Schema、内链、sitemap 和完整生产构建；确认五个 API 入口不变。
- [x] 提交并同步本轮经验证的全量产品标题扩展至 `master`。

> 审计结论（2026-08-27）：遵循 Google Search Central 对 `<title>` 的“唯一、准确、描述性且简洁”要求，标题生成器改为以清洁后的具体产品名称为主语，随后按材料、细度、款式/织法、颜色、纱线支数、包装、重量、应用、人群、季节和尺寸等真实字段递进附加，并仅在产品名过短时补充产品类别。所有标题均保留简短品牌尾缀；泛化促销词不再占用标题前部。目录字段覆盖为：材料/细度 582、颜色 582、织法或图案 158、季节 180、人群 150、尺寸 183、包装 78、重量 180、应用 582 条产品记录。六语各 582 个产品详情页均有 H1、canonical、hreflang 和 Product Schema，Offer 输出保持为 0；新增支数/包装/重量/应用等安全扩展规格已进入相应产品的 H1 与可用长度内的 `<title>`。

# 博客页面内容可见性故障诊断与恢复

- [x] 审计博客集合文件、语言目录、列表页、详情页、索引白名单和近期博客相关提交，确认内容未显示的实际原因。
- [x] 区分页面可见性、可索引性和历史文章内容风险，确保修复不会删除博客文件、覆盖正文或将未核验文章错误设为可索引。
- [x] 实施最小范围的博客列表或路由可见性修复，保留既有文章 URL、多语言路由和 noindex 策略。
- [x] 验证六语博客列表、可访问文章页、robots、canonical、hreflang、sitemap、生产构建和 API 入口。
- [x] 提交并同步经验证的博客内容可见性修复至 `master`，并说明实际原因。

> 修复结论（2026-08-27）：博客内容文件未被删除，`src/content/blog/` 中仍有 282 篇文章；问题出在博客列表页将“通过索引白名单”错误地作为“对访客可见”的筛选条件，导致仅显示 1 篇已审阅英文文章，其他语言显示为空。现已把文章可见性与搜索索引控制拆分：六语博客归档重新显示各自全部保留文章（en 66、cn 56、de/fr/ja/kr 各 40 篇），各文章仍按原有规则独立输出 robots；博客归档列表本身为 noindex，未审阅历史文章继续 noindex，只有已审阅的英文文章保留在 sitemap-blog。完整构建通过，未覆盖文章正文、未删除 URL，也未把历史文章错误提交为搜索入口。

# 近期羊绒行业动态博客与日期补全

- [x] 审计现有博客的发布日期、更新时间和可索引白名单，确定应补充的近期行业、贸易或合规主题与所需语言版本。
- [x] 核验不少于两项近期官方、行业组织或贸易监管来源，并记录发布日期、原始链接、事实边界和采购方相关性。
- [x] 撰写少量带明确日期、来源链接和项目适用范围的行业动态文章；仅陈述来源可支持的事实，不写入市场价格预测、企业能力或供应承诺。
- [x] 补全新增文章的发布/更新时间、引用、文章元数据、tags 与相关产品发现链接，并按审核状态设置正确的索引策略。
- [x] 验证六语文章路由、日期、可见性、robots、canonical、hreflang、Article Schema、sitemap、完整生产构建与 API 入口。
- [x] 提交并同步经验证的近期行业动态博客至 `master`，汇报原始来源和文章日期。

> 更新结论（2026-08-27）：新增两项近期欧盟纺织行业动态，均提供英中双语版本，发布日期和更新时间均为 2026-08-27：（1）欧盟 DPP 注册系统已于 2026-07-20 运行、纺织品专门要求预计于 2027 年第三至第四季度通过；（2）欧盟关于未售服装、配饰和鞋类销毁限制的措施中，大型企业适用日期为 2026-07-19。两项均直接引用欧盟委员会原始页面，并明确区分已发布事实、未来产品专门法规与项目级合规责任；没有声称本站、供应方或任何产品已满足 DPP、库存或循环合规要求。全库 286 篇博客现均有 `publishDate` 和 `updatedDate`；此前缺少更新时间的文章以其原有发布日期回填，不伪造近期更新时间。新增四篇动态文章均列入索引白名单，sitemap-blog 从 1 条增至 5 条，完整生产构建已通过。

# 博客 Schema.org JSON-LD 审计与优化

- [x] 审计博客详情页、博客归档、BaseLayout 和 FAQ 提取逻辑，以及已审阅与 noindex 文章的现有 JSON-LD 输出。
- [x] 对照可见标题、摘要、发布日期、更新时间、作者、图片、canonical、语言和面包屑，确定 BlogPosting、BreadcrumbList、FAQPage 与 ItemList 的一致性缺口。
- [x] 实施最小范围的结构化数据优化：仅为可索引且有对应可见内容的页面输出搜索增强 Schema，不加入评论、评分、价格、库存、供应范围或未经核验实体事实。
- [x] 对全量博客静态 HTML 审计 JSON-LD 的可解析性、类型、URL、日期与索引边界，并验证六语路由、sitemap、完整生产构建和 API 入口。
- [x] 提交并同步经验证的博客结构化数据优化至 `master`。

> 审计结论（2026-08-27）：全量静态审计覆盖 286 篇博客文章。5 篇索引白名单文章各输出 1 个 BlogPosting 与 1 个包含当前文章的 BreadcrumbList；其中 4 篇含可见 FAQ 的文章输出 1 个 FAQPage。281 篇 noindex 历史文章未输出 BlogPosting、BreadcrumbList 或 FAQPage。BlogPosting 现使用与 canonical 一致的 `url`、`mainEntityOfPage.@id` 和稳定 `@id`，并包含可见标题、摘要、发布日期、更新时间、作者、图像、语言和本地化 Blog 层级；中、日、韩内容不再输出基于空格分词的不可靠 `wordCount`。审计发现并解决了详情页原先“默认博客面包屑 + 专用文章面包屑”两条重复 BreadcrumbList 的问题。新增脚本 `scripts/seo/audit-blog-structured-data.mjs` 将持续检查 JSON-LD 可解析性、字段完整性、canonical 对齐和索引策略。未加入评价、评分、价格、库存、服务范围或未经核验企业事实。

# 核心页面移动端体验与产品发现路径增量优化

- [x] 审计首页、产品总目录、五个分类枢纽、代表产品页和可索引博客在桌面/手机端的首屏层级、行动入口、上下文内链与加载结构。
- [x] 识别只依赖真实目录字段和既有页面能力即可修复的移动端阅读、产品发现或性能瓶颈，排除未经核验的商业信息。
- [x] 实施最优先的响应式信息层级、可访问性、图片加载或可索引内链优化，不删除现有内容、产品 URL、多语言路由或询盘能力。
- [x] 验证桌面和手机视口的文字对比、交互、横向溢出、页面链接、H1、canonical、hreflang、Schema、sitemap、完整生产构建与 API 入口。
- [x] 提交并同步经验证的核心页面体验优化至 `master`。

> 优化结论（2026-08-27）：移动端首页首屏（390px）已确认标题、正文、主询盘和目录下载入口均可读且不被悬浮联络栏遮挡。首页可见定位由“源头工厂/工厂直供/成立年份”改为“羊绒产品资料/B2B 采购沟通”，六语主指标由不一致的 4 条路径统一为实际 5 个分类入口；原料模块的年份装饰卡也改为由现有分类数组驱动的路径计数。中文首页、原料和纱线页的 SEO 元关键词/标题已收敛为产品类别、纱线支数、颜色和规格等目录语义。分类组件当前安全主题已覆盖所有活跃类别；其运行时后备路径亦改为中性产品记录文案，避免旧的工厂、MOQ、价格、交期、市场覆盖和定制承诺在未来配置异常时重新输出。六语首页、英文/中文纱线分类及中文围巾、帽子、配饰分类静态验证均保留 H1、canonical、hreflang、FAQPage 或 ItemList；生产构建成功，sitemap 为 static=108、blog=5、products=3522，API 根入口为 5。

# 全站核心路径体验审阅与最高优先级优化

- [x] 在本地桌面预览审阅首页、产品总目录、代表产品详情页与博客归档，并以六语全量产品静态页核验核心内容、链接、抓取和询盘路径。
- [x] 按影响范围、任务阻塞程度、可信内容风险和修复成本排序候选问题，确认产品详情页旧版生产与商业断言为最高优先级，同时明确不触碰真实产品、URL、多语言、图片和询盘边界。
- [x] 停用高风险旧版详情模块，收敛摘要织法与六语元信息、页脚定位，保留真实规格、规格账本、质量控制路径、相关产品、加入询价单与询盘能力。
- [x] 验证全量产品静态页的旧断言、Offer/货币/库存 Schema 均为 0；抽检六语 H1、canonical、hreflang、Product Schema、询盘动作、表单与质量控制链接；确认 sitemap=static 108/blog 5/products 3522，API=5，并清理构建产物。
- [x] 提交并同步经验证的最高优先级体验优化至 `master`，在审阅记录中说明发现、改动理由与验证结果。

> 优化结论（2026-08-28）：核心路径审阅确认，产品详情页仍输出的旧版设备、检测、库存、交期、MOQ、价格、产能及实体背书文案会在所有产品 URL 中模糊“目录已列规格”和“项目级确认”的边界，因而优先级高于目录比较流程等界面改进。现已在调用端停用高风险详情区，避免重写或删除上游产品数据；摘要织法仅提取中性目录构造术语，元描述/关键词和页脚同步收敛。3,540 个多语言产品静态页中的指定旧断言、Offer、价格货币与库存字段均为 0，六语代表页保留现有可索引和询盘能力。

# 产品总目录筛选、比较与详情发现路径优化

- [x] 审计产品总目录的分类索引、筛选、搜索、排序、比较、产品卡片、询盘清单入口和六语元信息，定位采购方在“先筛选、再比较、进入详情”之间的决策阻塞点及未验证工厂身份断言。
- [x] 设计不新增商业承诺且保留现有筛选、比较、分页、产品 URL、多语言、图片与询盘逻辑的目录决策与元信息优化方案。
- [x] 实施目录操作层级、结果状态、比较承接、详情引导和元信息收敛等可见体验改进，不删除真实产品记录或既有功能。
- [x] 在本地桌面实测筛选、比较状态和详情链接文案；完成六语元信息、H1、canonical、hreflang、Schema、sitemap、完整生产构建和 API 入口静态验证。
- [x] 提交并同步经验证的产品目录决策路径优化至 `master`，记录实际发现与保留能力。

> 优化结论（2026-08-28）：目录原有筛选、比较和询盘功能可用，但比较意图仅在首次点选卡片后由底部栏解释，卡片主动作“获取报价”也实际通向产品详情页。现已在控制台前加入六语三步路径：先按已列属性筛选，再查看单款产品记录，最后按需比较最多三款；卡片主动作同步改为“查看产品记录”。比较栏、比较弹窗、分页、筛选、产品 URL、图像、加入询价单和主询盘入口均保留。目录页还移除了未使用客户端副标题数据及六语标题/关键词中的工厂直供或 OEM 断言。完整构建后，六语目录页的相关残余断言为 0，产品详情页既有可信内容与 Offer 边界复测亦为 0。

# 公开线上全量产品页 SEO 与 Schema 审计

- [x] 确定公开线上主域、可访问 sitemap 与全量产品 URL 清单，记录实际 HTTP 响应与抽样页面基线。
- [x] 批量抓取公开线上产品页，审计 `<title>`、元描述、canonical、hreflang、robots、Product JSON-LD、面包屑与常见解析错误。
- [x] 统计 Offer、价格货币、库存、旧设备/工厂或商业断言等不应线上输出的字段，并与 GitHub `master` 的已验证静态边界对照。
- [x] 修复产品详情模板同时输出默认面包屑和专用产品面包屑导致的重复 `BreadcrumbList`，不改变可见面包屑、产品 Schema、URL 或询盘能力。
- [x] 完成完整本地静态构建，并记录分类枢纽、超长描述和超时 URL 的审计口径；当前待 GitHub 同步后对公开线上代表产品页复核单一 Product 与单一 BreadcrumbList 输出。
- [ ] 输出线上审计结论，明确已生效、尚未生效、不可访问和需等待发布平台完成更新的项目；不直接操作线上环境。
