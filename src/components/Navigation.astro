---
/**
 * 顶部导航（响应式 + 多语言切换器）
 */
import { LOCALES, LOCALE_NAMES, LOCALE_FLAGS, type Locale } from '@/lib/i18n';
import { localePath } from '@/lib/i18n';

interface Props {
  locale: Locale;
  currentPath?: string;
}

const { locale, currentPath = '/' } = Astro.props;

// 主导航（每个语种本地化 label）
const navItems = [
  { href: localePath(locale, '/'), labelKey: 'nav.home' },
  { href: localePath(locale, '/products'), labelKey: 'nav.products' },
  { href: localePath(locale, '/raw-material'), labelKey: 'nav.rawMaterial' },
  { href: localePath(locale, '/yarn-fabric'), labelKey: 'nav.yarnFabric' },
  { href: localePath(locale, '/garment-oem'), labelKey: 'nav.garmentOem' },
  { href: localePath(locale, '/ordos-origin'), labelKey: 'nav.ordosOrigin' },
  { href: localePath(locale, '/factory'), labelKey: 'nav.factory' },
  { href: localePath(locale, '/download'), labelKey: 'nav.download' },
  { href: localePath(locale, '/blog'), labelKey: 'nav.blog' },
  { href: localePath(locale, '/contact'), labelKey: 'nav.contact' },
];

// 翻译文本
const labels: Record<string, Record<string, string>> = {
  en: { 'nav.home': 'Home', 'nav.products': 'Products', 'nav.rawMaterial': 'Raw Material', 'nav.yarnFabric': 'Yarn & Fabric', 'nav.garmentOem': 'Garment OEM', 'nav.ordosOrigin': 'Ordos Origin', 'nav.factory': 'Factory', 'nav.download': 'Download', 'nav.blog': 'Blog', 'nav.contact': 'Contact', 'nav.menuToggle': 'Menu' },
  de: { 'nav.home': 'Start', 'nav.products': 'Produkte', 'nav.rawMaterial': 'Rohmaterial', 'nav.yarnFabric': 'Garn & Stoff', 'nav.garmentOem': 'Bekleidung OEM', 'nav.ordosOrigin': 'Ordos Herkunft', 'nav.factory': 'Fabrik', 'nav.download': 'Download', 'nav.blog': 'Blog', 'nav.contact': 'Kontakt', 'nav.menuToggle': 'Menü' },
  fr: { 'nav.home': 'Accueil', 'nav.products': 'Produits', 'nav.rawMaterial': 'Matière Première', 'nav.yarnFabric': 'Fil & Tissu', 'nav.garmentOem': 'Vêtement OEM', 'nav.ordosOrigin': 'Origine Ordos', 'nav.factory': 'Usine', 'nav.download': 'Télécharger', 'nav.blog': 'Blog', 'nav.contact': 'Contact', 'nav.menuToggle': 'Menu' },
  ja: { 'nav.home': 'ホーム', 'nav.products': '製品', 'nav.rawMaterial': '原料', 'nav.yarnFabric': '糸・生地', 'nav.garmentOem': '衣料OEM', 'nav.ordosOrigin': 'オルドス起源', 'nav.factory': '工場', 'nav.download': 'ダウンロード', 'nav.blog': 'ブログ', 'nav.contact': 'お問い合わせ', 'nav.menuToggle': 'メニュー' },
  kr: { 'nav.home': '홈', 'nav.products': '제품', 'nav.rawMaterial': '원료', 'nav.yarnFabric': '원사·직물', 'nav.garmentOem': '의류 OEM', 'nav.ordosOrigin': '오르도스 원산지', 'nav.factory': '공장', 'nav.download': '다운로드', 'nav.blog': '블로그', 'nav.contact': '문의', 'nav.menuToggle': '메뉴' },
  cn: { 'nav.home': '首页', 'nav.products': '产品', 'nav.rawMaterial': '羊绒原料', 'nav.yarnFabric': '纱线/面料', 'nav.garmentOem': '成衣代工', 'nav.ordosOrigin': '鄂尔多斯产地', 'nav.factory': '工厂实力', 'nav.download': '下载中心', 'nav.blog': '博客', 'nav.contact': '联系我们', 'nav.menuToggle': '菜单' },
};

const t = (key: string) => labels[locale]?.[key] || labels.en[key] || key;
---

<header class="sticky top-0 z-40 w-full border-b border-border bg-background shadow-sm">
  <div class="container-narrow flex h-16 items-center justify-between">
    <!-- Logo -->
    <a href={localePath(locale, '/')} class="flex items-center gap-2 group">
      <span class="font-display text-2xl font-medium tracking-tight text-primary">
        DONGXIAO<sup class="text-xs">®</sup>
      </span>
      <span class="hidden md:inline text-xs text-muted-foreground tracking-wider uppercase">
        Cashmere · Ordos
      </span>
    </a>

    <!-- Desktop Nav -->
    <nav class="hidden lg:flex items-center gap-6 text-sm">
      {navItems.slice(0, 8).map((item) => (
        <a
          href={item.href}
          class:list={[
            'transition-colors hover:text-primary',
            currentPath === item.href ? 'text-primary font-medium' : 'text-muted-foreground',
          ]}
        >
          {t(item.labelKey)}
        </a>
      ))}
    </nav>

    <!-- Right side -->
    <div class="flex items-center gap-3">
      <!-- Language Switcher -->
      <div class="relative group">
        <button
          type="button"
          class="inline-flex items-center gap-1.5 text-sm text-muted-foreground hover:text-primary transition-colors"
          aria-label="Change language"
        >
          <span>{LOCALE_FLAGS[locale]}</span>
          <span class="uppercase">{locale}</span>
          <svg class="w-3 h-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
          </svg>
        </button>
        <div class="absolute right-0 top-full mt-2 w-48 rounded-md border border-border bg-popover shadow-lg opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all">
          {LOCALES.map((loc) => (
            <a
              href={localePath(loc, currentPath.replace(`/${locale}`, '') || '/')}
              class:list={[
                'flex items-center gap-2 px-4 py-2 text-sm hover:bg-accent transition-colors',
                loc === locale ? 'bg-accent font-medium' : '',
              ]}
            >
              <span>{LOCALE_FLAGS[loc]}</span>
              <span>{LOCALE_NAMES[loc]}</span>
            </a>
          ))}
        </div>
      </div>

      <!-- CTA: Contact -->
      <a
        href={localePath(locale, '/contact')}
        class="hidden sm:inline-flex items-center justify-center rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground hover:bg-primary/90 transition-colors"
      >
        {locale === 'cn' ? '立即询盘' : 'Get Quote'}
      </a>

      <!-- Mobile Menu Toggle -->
      <button
        type="button"
        class="lg:hidden p-2"
        aria-label={t('nav.menuToggle')}
        id="mobile-menu-toggle"
      >
        <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
        </svg>
      </button>
    </div>
  </div>

  <!-- Mobile Menu -->
  <nav id="mobile-menu" class="lg:hidden hidden border-t border-border bg-background">
    <div class="container-narrow py-4 space-y-1">
      {navItems.map((item) => (
        <a
          href={item.href}
          class="block py-2 text-sm text-foreground hover:text-primary transition-colors"
        >
          {t(item.labelKey)}
        </a>
      ))}
    </div>
  </nav>
</header>

<script is:inline>
  document.getElementById('mobile-menu-toggle')?.addEventListener('click', () => {
    document.getElementById('mobile-menu')?.classList.toggle('hidden');
  });
</script>
